#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run code quality checks before commit
bun run format:check
bun run lint

# Note: type-check temporarily disabled due to known LangChain generic type issues
# These will be resolved in a future update. CI will still run type-check.
# bun run type-check
